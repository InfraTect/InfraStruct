/**
 * 사용자(개발자)가 직접 import 해서 쓰는 공개 API.
 *
 * <p>예: {@code InfraStruct.run(...)}, {@code @InfraStructApplication}, {@code @Resource},
 * {@code @Required}.
 *
 * <p>여기를 바꾸면 사용자 코드가 깨진다 → 배포 후에는 신중하게 버전업할 것.
 */
package com.infrastruct.api;
